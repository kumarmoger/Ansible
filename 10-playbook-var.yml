---
 - hosts: localhost
   become: true
   gather_facts: yes
   vars: 
    pkg_name: httpd
   tasks:
     - name: install package
       yum:
         name: "{{pkg_name}}"
         state: latest
     - name: print a message
       debug:
         msg: "{{pkg_name}} installed"
...